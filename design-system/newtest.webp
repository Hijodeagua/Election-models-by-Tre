/* Election Oracle — Webfonts
   Display face: DM Serif Display (warm editorial headlines)
   Loaded from Google Fonts CDN. Consumers linking styles.css get these
   automatically; HTML cards/kits also add a <link> for belt-and-suspenders.
   NOTE: substituted from Google Fonts — swap in licensed brand binaries here
   if/when available (replace this @import with local @font-face rules). */

@import url('https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=Source+Serif+4:opsz,wght@8..60,400;8..60,500;8..60,600&display=swap');
